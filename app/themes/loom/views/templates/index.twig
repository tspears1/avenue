{% extends 'layouts/base.twig' %}

{% block content %}
    <h1>{{ post.title }} - Updated on {{ post.date|date("F j, Y") }}</h1>
    <section style="display: grid; gap: 2rem;">
        {{ post.content }}
    </section>
{% endblock %}
